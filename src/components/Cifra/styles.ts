import styled from 'styled-components/native';

export const Container = styled.View`
    justify-content: center;
    align-items: center;
`;

export const Row = styled.View`
    flex-direction: row;
    align-items: flex-end;
`;

export const CentsContainer = styled.View`
    position: absolute;
    right: -30px;
    bottom: 10px;
`;